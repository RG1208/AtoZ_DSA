#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a[5] = {2, 55, 33, 7, 2};

    sort(a, a + 5);
    // sort(v.begin(),v.end())   iin case of vectors
    // sort(a+2,a+4)   if we want to sort in between an array/vector

    return 0;
}